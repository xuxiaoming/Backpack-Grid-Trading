version: "3.9"

services:
  standx-bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    # 去掉 volumes 挂载，避免文件夹冲突
    restart: always
    command: [
      "--exchange", "standx",
      "--market-type", "perp",
      "--symbol", "BTC-USD",
      "--strategy", "standard",
      "--spread", "0.06",
      "--quantity", "0.02",
      "--max-orders", "1",
      "--inventory-skew", "0.8",
      "--max-position", "0.1",
      "--target-position", "0",
      "--interval", "6",
      "--enable-rebalance",
      "--leverage", "6"
    ]
