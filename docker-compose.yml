version: "3.9"

services:
  backpack-bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    volumes:
      - ./market_maker.log:/app/market_maker.log
      - ./orders.db:/app/orders.db
    ports:
      - "5000:5000"
    command: ["--web"]
